// @glow
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  topBarRoot: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingHorizontal: 8
  },
  title: {
    flex: 1,
    textAlign: "center",
    fontSize: 18,
    color: "#000"
  },
  emptyBtn: {
    width: 36
  }
});
